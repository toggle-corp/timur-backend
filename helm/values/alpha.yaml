app:
  environment: ALPHA

  # DB (CNPG) + Redis (Dragonfly) connection env come from values/operators.yaml.

  # S3 storage is enabled by default in values.yaml. The endpoint, bucket names
  # and credentials are deployment-specific and supplied per-environment at
  # deploy time (see main/settings.py for the AWS_S3_* options).
  # env: {}

  # DJANGO_SECRET_KEY, S3 credentials, Google + Sentry secrets are provided
  # per-environment at deploy time.
  secrets: {}
